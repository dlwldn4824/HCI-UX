# 🚀 배포 시 변경해야 할 URL 위치 가이드

이 문서는 로컬 개발 환경에서 서버 배포 환경으로 전환할 때 수정해야 할 모든 URL과 설정 위치를 정리한 것입니다.

## 🌐 배포 정보

- **프론트엔드 배포 URL:** https://tellme.kwidea.com/
- **백엔드 API 서버:** (백엔드 서버 URL을 확인하여 업데이트 필요)
- **WebSocket 서버:** (WebSocket 서버 URL을 확인하여 업데이트 필요)

---

## 📋 목차

1. [프론트엔드 (React) 설정](#1-프론트엔드-react-설정)
2. [백엔드 (Node.js/Express) 설정](#2-백엔드-nodejsexpress-설정)
3. [Android 앱 (Capacitor) 설정](#3-android-앱-capacitor-설정)
4. [환경 변수 파일](#4-환경-변수-파일)
5. [하드코딩된 URL 수정](#5-하드코딩된-url-수정)

---

## 1. 프론트엔드 (React) 설정

### ✅ 1-1. 환경 변수 파일 생성 (추천 방법)

**파일 위치:** `co-show/.env.production`

```env
# 백엔드 API 서버 URL (프로덕션)
VITE_API_BASE_URL=https://your-backend-server.com

# WebSocket 서버 URL (프로덕션)
VITE_WS_BASE_URL=wss://your-backend-server.com

# 프론트엔드 서버 URL (프로덕션)
VITE_FRONTEND_URL=https://tellme.kwidea.com

# 사진 업로드 서버 URL (프로덕션)
VITE_PHOTO_UPLOAD_URL=https://your-photo-server.com
```

> **참고:** Vite는 `VITE_` 접두사가 붙은 환경 변수만 클라이언트에 노출됩니다.

---

### ✅ 1-2. API 베이스 URL 설정

**파일:** `co-show/src/services/apiBase.js`

현재 코드는 이미 환경 변수를 지원하므로, `.env.production`에 `VITE_API_BASE_URL`만 설정하면 됩니다.

```javascript:1:16:co-show/src/services/apiBase.js
export function getApiBase() {
  // 1) .env에 명시된 경우 (Temi 실기기나 배포 시)
  const envBase = import.meta?.env?.VITE_API_BASE_URL;
  if (envBase && String(envBase).trim() !== "") {
    return envBase;
  }

  // 2) 안드로이드 환경이면 에뮬레이터용 주소 사용
  if (typeof navigator !== "undefined" && /Android/i.test(navigator.userAgent)) {
    // Android Emulator -> PC localhost:4000
    return "http://10.0.2.2:4000";
  }

  // 3) 그 외(브라우저 개발) -> 로컬 PC에서 백엔드
  return "http://localhost:4000";
}
```

**✅ 수정 불필요** - 환경 변수로 해결 가능

---

### ✅ 1-3. 문의 수집기 URL 수정

**파일:** `co-show/src/services/inquiryCollector.js`

```javascript:1:10:co-show/src/services/inquiryCollector.js
function getApiBase() {
  const envBase = import.meta?.env?.VITE_API_BASE_URL;
  if (envBase && String(envBase).trim() !== "") return envBase;

  if (typeof navigator !== "undefined" && /Android/i.test(navigator.userAgent)) {
    return "http://192.168.0.2:4000"; // 에뮬레이터용
  }

  return "http://192.168.0.2:4000"; // 웹 개발 환경
}
```

**✅ 수정 불필요** - 이미 환경 변수 지원함

---

### ✅ 1-4. WebSocket 클라이언트 URL

**파일:** `co-show/src/lib/temiSocket.js`

```javascript:8:14:co-show/src/lib/temiSocket.js
class TemiSocketClient {
  constructor(serverUrl = "http://localhost:4000") {
    this.socket = null;
    this.serverUrl = serverUrl;
    this.isConnected = false;
    this.listeners = new Map();
  }
```

**✅ 수정 필요:** 기본값을 환경 변수로 변경

```javascript
// 수정 예시:
constructor(serverUrl = import.meta?.env?.VITE_WS_BASE_URL || "http://localhost:4000") {
```

**또는:** `co-show/src/components/TemiController.jsx`에서 사용할 때:

```javascript:11:11:co-show/src/components/TemiController.jsx
  const socket = useTemiSocket("http://localhost:4000");
```

**✅ 수정 필요:** 환경 변수 사용

```javascript
const socket = useTemiSocket(import.meta?.env?.VITE_WS_BASE_URL || "http://localhost:4000");
```

---

## 2. 백엔드 (Node.js/Express) 설정

### ✅ 2-1. CORS 설정

**파일:** `co-show/backend/src/app.ts`

```javascript:14:14:co-show/backend/src/app.ts
app.use(cors({ origin: true, credentials: true })); // 개발 편의용,  origin: ["http://localhost:5173", "http://222.232.30.11:4000"]
```

**✅ 수정 필요:** 프로덕션에서는 특정 도메인만 허용

```javascript
// 프로덕션 환경에서는:
app.use(cors({ 
  origin: process.env.FRONTEND_URL || "https://tellme.kwidea.com",
  credentials: true 
}));
```

---

### ✅ 2-2. 서버 포트 설정

**파일:** `co-show/backend/src/server.ts`

```javascript:5:5:co-show/backend/src/server.ts
const PORT = Number(process.env.PORT || 4000);
```

**✅ 수정 불필요** - 이미 환경 변수 사용 중

**환경 변수 설정:**
```env
PORT=4000
```

---

## 3. Android 앱 (Capacitor) 설정

### ✅ 3-1. Capacitor 설정 파일

**파일:** `co-show/capacitor.config.json`

```json:5:9:co-show/capacitor.config.json
  "server": {
    "androidScheme": "http",
    "url": "http://192.0.0.2:5173/",
    "cleartext": true
  }
```

**✅ 수정 필요:** 배포 서버 URL로 변경

```json
{
  "appId": "com.example.coshowsample",
  "appName": "co-show",
  "webDir": "dist",
  "server": {
    "androidScheme": "https",
    "url": "https://tellme.kwidea.com/",
    "cleartext": false
  }
}
```

> **참고:** HTTPS를 사용하면 `cleartext: false`로 변경해야 합니다.

---

### ✅ 3-2. Android MainActivity URL (선택사항)

**파일:** `co-show/android/app/src/main/java/com/example/MainActivity.java`

개발 중에만 사용하는 로드 URL이 있다면 제거하거나 주석 처리하세요.

---

## 4. 환경 변수 파일

### ✅ 4-1. 프론트엔드 환경 변수

**파일 생성:** `co-show/.env.production`

```env
# 백엔드 API 서버
VITE_API_BASE_URL=https://api.your-server.com

# WebSocket 서버
VITE_WS_BASE_URL=wss://api.your-server.com

# 프론트엔드 서버
VITE_FRONTEND_URL=https://tellme.kwidea.com

# 사진 업로드 서버 (별도 서버인 경우)
VITE_PHOTO_UPLOAD_URL=https://photo.your-server.com
```

---

### ✅ 4-2. 백엔드 환경 변수

**파일 생성:** `co-show/backend/.env.production`

```env
# 서버 포트
PORT=4000

# 프론트엔드 URL (CORS용)
FRONTEND_URL=https://tellme.kwidea.com

# 데이터베이스 URL (Prisma)
DATABASE_URL="file:./dev.db"

# 기타 환경 변수...
```

---

## 5. 하드코딩된 URL 수정

### ✅ 5-1. 사진 업로드/다운로드 URL

**파일:** `co-show/src/pages/PhotoFilter.jsx`

이 파일은 이미 환경 변수를 지원하도록 수정되었습니다. `VITE_PHOTO_UPLOAD_URL` 환경 변수를 설정하면 됩니다.

**✅ Vercel 환경 변수 설정 방법:**

1. Vercel 대시보드 → 프로젝트 → Settings → Environment Variables
2. 다음 변수 추가:
   - **Key:** `VITE_PHOTO_UPLOAD_URL`
   - **Value:** `http://44.198.30.193:8080`
   - **Environment:** Production (필요시 Preview, Development도 추가)

**⚠️ 참고:** 
- HTTPS 환경에서 HTTP 서버에 접근하면 Mixed Content 경고가 발생할 수 있습니다.
- Android WebView에서는 `cleartext` 설정으로 HTTP 접근이 허용됩니다.
- 일반 브라우저에서는 Mixed Content 정책에 의해 차단될 수 있으므로, 가능하면 HTTPS 서버를 사용하는 것을 권장합니다.

---

### ❌ 5-2. Vercel 설정 (프록시)

**파일:** `co-show/vercel.config`

```json:6:6:co-show/vercel.config
      "destination": "http://44.198.30.193:8080/$1"
```

**✅ 수정 필요:** 배포 서버 URL로 변경

```json
{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://your-backend-server.com/$1"
    }
  ]
}
```

---

### ✅ 5-3. 비디오 URL (Supabase - 수정 불필요)

**파일들:**
- `co-show/src/pages/TemiGuide.jsx`
- `co-show/src/pages/QuizCorrect.jsx`
- `co-show/src/pages/QuizWrong.jsx`

이 파일들은 Supabase Storage URL을 사용하므로 **수정 불필요**합니다. Supabase는 이미 배포된 CDN이므로 그대로 사용하면 됩니다.

```javascript
// 예시:
const GUIDE_VIDEO_URL = "https://fxymkjkckqsgxdzbhcxl.supabase.co/storage/v1/object/public/videos/follow.mov";
```

---

## 📝 체크리스트

배포 전 확인 사항:

- [ ] `co-show/.env.production` 파일 생성 및 환경 변수 설정
- [ ] `co-show/backend/.env.production` 파일 생성 및 환경 변수 설정
- [ ] `co-show/src/lib/temiSocket.js` - WebSocket URL 환경 변수 사용
- [ ] `co-show/src/components/TemiController.jsx` - WebSocket URL 환경 변수 사용
- [ ] `co-show/src/pages/PhotoFilter.jsx` - 사진 업로드 URL 환경 변수 사용
- [ ] `co-show/backend/src/app.ts` - CORS 설정 수정
- [ ] `co-show/capacitor.config.json` - 서버 URL 변경
- [ ] `co-show/vercel.config` - 프록시 목적지 URL 변경 (Vercel 사용 시)

---

## 🔧 빌드 및 배포 명령어

### 프론트엔드 빌드
```bash
cd co-show
npm run build  # 프로덕션 빌드 (자동으로 .env.production 사용)
```

### 백엔드 시작
```bash
cd co-show/backend
npm run dev  # 개발 모드
# 또는
NODE_ENV=production npm start  # 프로덕션 모드
```

---

## 💡 추가 팁

1. **환경별 설정 파일:**
   - `.env.development` - 개발 환경
   - `.env.production` - 프로덕션 환경
   - `.env.local` - 로컬 환경 (git에 커밋하지 않기)

2. **환경 변수 보안:**
   - 민감한 정보(API 키, DB 비밀번호 등)는 환경 변수로 관리
   - `.env` 파일은 `.gitignore`에 추가

3. **Capacitor 재빌드:**
   - `capacitor.config.json` 수정 후:
   ```bash
   npx cap sync
   ```

4. **Android 앱 재빌드:**
   - Capacitor 설정 변경 후 APK를 다시 빌드해야 함

---

## 📞 문제 발생 시

1. 환경 변수가 제대로 로드되지 않으면 브라우저 콘솔에서 `import.meta.env` 확인
2. CORS 오류 발생 시 백엔드 CORS 설정 확인
3. WebSocket 연결 실패 시 서버가 WebSocket을 지원하는지 확인 (HTTPS/WSS 필수)

